<!DOCTYPE html>

<title>Edit Vehicle</title>

{% block body %}
    <h1>Edit Vehicle</h1>

    {{ include('yann/vehicle-form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_vehicle_index') }}">back to list</a>

    {{ include('vehicle/_delete_form.html.twig') }}
{% endblock %}
